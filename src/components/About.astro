<section id="about" class="about-section">
    <div class="about-noise"></div>

    <div class="about-container">
        <div class="about-header">
            <div class="section-meta">
                <span class="meta-label">/ ABOUT</span>
            </div>

            <h2 class="philosophy-title">
                SYSTEMS<br />
                THINKING.<br />
                CINEMATIC<br />
                EXECUTION.
            </h2>
            <div class="title-underline"></div>
        </div>

        <div class="about-content">
            <p class="lead-text">
                Head of Motion Design & Systems with 10+ years of experience
                leading motion direction, scalable visual systems, and
                production-ready workflows across digital, interactive, and
                AI-enabled environments.
            </p>

            <p class="body-text">
                I combine systems-level leadership with hands-on execution and a
                decade of teaching motion design at university level — which
                means I don't just set direction, I build the logic that lets
                teams understand, own, and scale it.
            </p>

            <div class="taxonomy-section">
                <h4 class="taxonomy-label">TAXONOMY KEYWORDS:</h4>
                <div class="taxonomy-list">
                    <span class="keyword">MOTION DIRECTION</span>
                    <span class="separator mono">/</span>
                    <span class="keyword">SYSTEM ARCHITECTURE</span>
                    <span class="separator mono">/</span>
                    <span class="keyword">CREATIVE LEADERSHIP</span>
                    <span class="separator mono">/</span>
                    <span class="keyword">AI WORKFLOWS</span>
                    <span class="separator mono">/</span>
                    <span class="keyword">HANDS-ON APPROACH</span>
                    <span class="separator mono">/</span>
                    <span class="keyword">LOTTIE-JSON</span>
                    <span class="separator mono">/</span>
                    <span class="keyword">MENTORSHIP AT SCALE</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    /* =========================================
       BASE / DESKTOP
       ========================================= */
    .about-section {
        color: var(--color-dark);
        padding: 8rem 2rem 10rem;
        display: flex;
        justify-content: center;
        position: relative;
        background-color: transparent;
    }

    /* Fondo base blanco/gris */
    .about-section::before {
        content: "";
        position: absolute;
        top: -800px;
        bottom: 0;
        left: 0;
        width: 100%;
        background-color: #f5f5f7;
        z-index: -3;
    }

    /* Imagen de fondo escalada a tamaño de diseño */
    .about-section::after {
        content: "";
        position: absolute;
        top: -800px;
        bottom: 0;
        left: 0;
        width: 100%;
        background-image: url("../assets/images/pattern.png");
        /* Cambio a un límite en píxeles para evitar el zoom excesivo */
        background-size: 1300px auto;
        background-repeat: no-repeat;
        background-position: center center;
        opacity: 0.6;
        pointer-events: none;
        z-index: -2;
    }

    /* Capa de granulado gris claro */
    .about-noise {
        position: absolute;
        top: -800px;
        bottom: 0;
        left: 0;
        width: 100%;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='2.5' numOctaves='4' stitchTiles='stitch'/%3E%3CfeColorMatrix type='saturate' values='0'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
        opacity: 0.4;
        mix-blend-mode: multiply;
        pointer-events: none;
        z-index: -1;
    }

    .about-container {
        width: 100%;
        max-width: 1200px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        position: relative;
        z-index: 10;
    }

    .section-meta {
        font-family: var(--font-mono);
        font-size: 14px;
        color: var(--color-accent);
        letter-spacing: 0.1em;
        margin-bottom: 1rem;
        display: flex;
        gap: 0.5rem;
    }

    .philosophy-title {
        font-family: var(--font-display);
        font-weight: 900;
        font-size: 96px;
        line-height: 0.9;
        letter-spacing: -0.03em;
        margin-bottom: 1.5rem;
        color: #000;
    }

    .title-underline {
        width: 60px;
        height: 2px;
        background-color: var(--color-accent);
        margin-top: 1rem;
    }

    .about-content {
        padding-top: 3rem;
    }

    .lead-text {
        font-family: var(--font-body);
        font-weight: 500;
        font-size: 20px;
        line-height: 1.4;
        margin-bottom: 2rem;
        color: #1a1a1a;
    }

    .body-text {
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 18px;
        line-height: 1.6;
        color: #333;
        opacity: 0.85;
        margin-bottom: 3rem;
    }

    .taxonomy-section {
        margin-top: 3rem;
    }

    .taxonomy-label {
        font-family: var(--font-mono);
        color: var(--color-accent);
        font-size: 15px;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        font-weight: 600;
        margin-bottom: 0.25rem;
    }

    .taxonomy-list {
        font-family: var(--font-mono);
        color: #424242;
        font-size: 15px;
        line-height: 1.7;
        font-weight: 500;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 0 0.2rem;
    }

    .separator.mono {
        color: var(--color-accent);
        font-family: var(--font-mono);
        font-weight: 400;
        font-size: 14px;
        margin: 0 0.4rem;
        display: inline-block;
        transform: translateY(-1px);
    }

    /* =========================================
       TABLET (769px a 1024px)
       ========================================= */
    @media (min-width: 769px) and (max-width: 1024px) {
        .section-meta {
            font-size: 14px;
        }
        .philosophy-title {
            font-size: 68px;
        }
        .lead-text {
            font-size: 19px;
        }
        .body-text {
            font-size: 17px;
        }
        .taxonomy-label {
            font-size: 14px;
        }
        .taxonomy-list {
            font-size: 14px;
        }
    }

    /* =========================================
       MOBILE (Hasta 768px)
       ========================================= */
    @media (max-width: 768px) {
        .about-section {
            padding: 2rem 1.5rem;
        }

        .about-section::after {
            background-size: cover; /* En móvil se necesita cover para que abarque la altura */
            background-position: center top;
        }

        .about-container {
            grid-template-columns: 1fr;
            gap: 2rem;
        }
        .section-meta {
            font-size: 14px;
        }
        .philosophy-title {
            font-size: 42px;
        }
        .about-content {
            padding-top: 0;
        }
        .lead-text {
            font-size: 18px;
        }
        .body-text {
            font-size: 16px;
        }
        .taxonomy-label {
            font-size: 14px;
        }
        .taxonomy-list {
            font-size: 14px;
        }
    }
</style>
